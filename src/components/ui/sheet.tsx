import { makeButton, makeDiv, makeImg, makeInput, makeSpan, makeTextarea } from "./stub";

export const Sheet = makeDiv("Sheet");
export const SheetContent = makeDiv("SheetContent");
export const SheetDescription = makeDiv("SheetDescription");
export const SheetHeader = makeDiv("SheetHeader");
export const SheetTitle = makeDiv("SheetTitle");
export const SheetTrigger = makeButton("SheetTrigger");
