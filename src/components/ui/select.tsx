import { makeButton, makeDiv, makeImg, makeInput, makeSpan, makeTextarea } from "./stub";

export const Select = makeDiv("Select");
export const SelectContent = makeDiv("SelectContent");
export const SelectItem = makeDiv("SelectItem");
export const SelectTrigger = makeButton("SelectTrigger");
export const SelectValue = makeSpan("SelectValue");
