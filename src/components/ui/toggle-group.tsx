import { makeButton, makeDiv, makeImg, makeInput, makeSpan, makeTextarea } from "./stub";

export const ToggleGroup = makeDiv("ToggleGroup");
export const ToggleGroupItem = makeButton("ToggleGroupItem");
