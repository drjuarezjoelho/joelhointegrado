import { makeButton, makeDiv, makeImg, makeInput, makeSpan, makeTextarea } from "./stub";

export const Alert = makeDiv("Alert");
export const AlertDescription = makeDiv("AlertDescription");
export const AlertTitle = makeDiv("AlertTitle");
