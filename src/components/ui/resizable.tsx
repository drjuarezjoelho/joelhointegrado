import { makeButton, makeDiv, makeImg, makeInput, makeSpan, makeTextarea } from "./stub";

export const ResizableHandle = makeDiv("ResizableHandle");
export const ResizablePanel = makeDiv("ResizablePanel");
export const ResizablePanelGroup = makeDiv("ResizablePanelGroup");
