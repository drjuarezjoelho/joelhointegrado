import { makeButton, makeDiv, makeImg, makeInput, makeSpan, makeTextarea } from "./stub";

export const ContextMenu = makeDiv("ContextMenu");
export const ContextMenuContent = makeDiv("ContextMenuContent");
export const ContextMenuItem = makeButton("ContextMenuItem");
export const ContextMenuTrigger = makeDiv("ContextMenuTrigger");
