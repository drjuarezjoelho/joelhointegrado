import { makeButton, makeDiv, makeImg, makeInput, makeSpan, makeTextarea } from "./stub";

export const Dialog = makeDiv("Dialog");
export const DialogContent = makeDiv("DialogContent");
export const DialogDescription = makeDiv("DialogDescription");
export const DialogFooter = makeDiv("DialogFooter");
export const DialogHeader = makeDiv("DialogHeader");
export const DialogTitle = makeDiv("DialogTitle");
export const DialogTrigger = makeButton("DialogTrigger");
