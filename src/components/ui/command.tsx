import { makeButton, makeDiv, makeImg, makeInput, makeSpan, makeTextarea } from "./stub";

export const Command = makeDiv("Command");
export const CommandEmpty = makeDiv("CommandEmpty");
export const CommandGroup = makeDiv("CommandGroup");
export const CommandInput = makeInput("CommandInput");
export const CommandItem = makeDiv("CommandItem");
export const CommandList = makeDiv("CommandList");
