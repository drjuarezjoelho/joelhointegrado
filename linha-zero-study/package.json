{
  "name": "linha-zero-study",
  "private": true,
  "type": "module",
  "description": "Coleta de dados — Projeto Linha Zero (publicação científica)",
  "scripts": {
    "db:generate": "drizzle-kit generate",
    "db:push": "drizzle-kit push",
    "db:migrate": "tsx scripts/apply-migrations.ts",
    "db:studio": "drizzle-kit studio",
    "db:seed": "tsx scripts/seed-coauthors.ts",
    "dev": "echo \"Implementar: concurrently web + server\" && exit 1",
    "build": "echo \"Implementar: vite build\" && exit 1"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "better-sqlite3": "^11.6.0",
    "dotenv": "^17.4.2",
    "drizzle-orm": "^0.38.0"
  },
  "devDependencies": {
    "@types/bcryptjs": "^2.4.6",
    "@types/better-sqlite3": "^7.6.11",
    "@types/node": "^22.0.0",
    "drizzle-kit": "^0.31.9",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
